{
  "name": "sain-real-asset-market",
  "version": "1.34.0",
  "private": true,
  "description": "SAIN Real Asset Market persistent onboarding and marketplace read model",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test --test-concurrency=1",
    "test:edx": "node --test --test-concurrency=1 test/edx-pipeline.test.js",
    "test:integration": "node --test --test-concurrency=1 test/edx-pipeline.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "openai": "^5.20.0",
    "pg": "^8.13.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  }
}
