import assert from 'node:assert/strict';import test from 'node:test';import { CounterpartyOperationsService } from '../services/counterparty-operations-service.js';
class D{constructor(){this.m=new Map();}k(t,i){return `${t}:${i}`;}list(t){return [...this.m.entries()].filter(([k])=>k.startsWith(`${t}:`)).map(([,v])=>structuredClone(v));}async put(t,i,p){this.m.set(this.k(t,i),structuredClone(p));}}
const reasoning={reasonForExportPackage(){return{unresolvedFields:[],unresolvedServicingFields:[]};}};const outcomes={summary(){return{status:'EXTERNAL_ACTIVITY_RECORDED'};}};
for(const [topic,text] of [['FINANCING_TERMS_CHANGE','Change the repayment terms'],['SETTLEMENT_AUTHORIZATION','Authorize settlement'],['EXTERNAL_TRANSFER_EXECUTION','Execute the transfer'],['INSTRUMENT_ISSUANCE','Issue the instrument'],['OWNERSHIP_TRANSFER','Transfer ownership'],['PAYMENT_AUTHORIZATION','Authorize payment']])test(`reserves ${topic} for principal authority`,async()=>{const d=new D();await d.put('EXPORT_PACKAGE','EXP',{id:'EXP',exportPackageId:'EXP',financingTransactionId:'FTX',state:'READY_FOR_SETTLEMENT_INSTRUCTION'});await d.put('TRANSACTION_PARTICIPATION_EVENT',topic,{id:topic,eventId:topic,windowId:'W',exportPackageId:'EXP',eventType:'PROCESSING_CLARIFICATION_REQUESTED',summary:text,details:{topic},createdAt:new Date().toISOString()});const s=new CounterpartyOperationsService(d,{reasoning,outcomes});const r=await s.resolveParticipationEvent(topic);assert.equal(r.authorityRequired,true);assert.equal(r.status,'AWAITING_PRINCIPAL_AUTHORITY');});
