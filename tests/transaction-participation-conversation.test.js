import assert from 'node:assert/strict';
import test from 'node:test';
import { CounterpartyOperationsService } from '../services/counterparty-operations-service.js';
class D{constructor(){this.m=new Map();}k(t,i){return `${t}:${i}`;}list(t){return [...this.m.entries()].filter(([k])=>k.startsWith(`${t}:`)).map(([,v])=>structuredClone(v));}async put(t,i,p){this.m.set(this.k(t,i),structuredClone(p));return p;}}
const reasoning={reasonForExportPackage(){return{unresolvedFields:[],unresolvedServicingFields:[]};}};const outcomes={summary(){return{status:'AWAITING_EXTERNAL_CONFIRMATION'};}};
test('counterparty response records grounding and does not claim external verification',async()=>{const d=new D();await d.put('EXPORT_PACKAGE','EXP-C',{id:'EXP-C',exportPackageId:'EXP-C',financingTransactionId:'FTX-C',state:'READY_FOR_SETTLEMENT_INSTRUCTION',settlementMethod:'ACH'});await d.put('TRANSACTION_PARTICIPATION_EVENT','EV-C',{id:'EV-C',eventId:'EV-C',windowId:'W-C',exportPackageId:'EXP-C',financingTransactionId:'FTX-C',eventType:'PROCESSING_CLARIFICATION_REQUESTED',summary:'How do we process this ACH?',details:{topic:'ACH'},createdAt:new Date().toISOString()});const s=new CounterpartyOperationsService(d,{reasoning,outcomes});const r=await s.resolveParticipationEvent('EV-C');assert.equal(r.groundedIn.outcomeStatus,'AWAITING_EXTERNAL_CONFIRMATION');assert.match(r.response,/External completion has not yet been independently verified/);assert.equal(r.groundedIn.exportPackageId,'EXP-C');});
