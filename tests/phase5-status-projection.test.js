import assert from 'node:assert/strict';import test from 'node:test';import { CounterpartyOperationsService } from '../services/counterparty-operations-service.js';class D{constructor(){this.m=new Map();}k(t,i){return `${t}:${i}`;}list(t){return [...this.m.entries()].filter(([k])=>k.startsWith(`${t}:`)).map(([,v])=>structuredClone(v));}async put(t,i,p){this.m.set(this.k(t,i),structuredClone(p));}}
test('status projection surfaces principal authority cases',async()=>{const d=new D();await d.put('COUNTERPARTY_OPERATION_CASE','C',{id:'C',caseId:'C',exportPackageId:'EXP',authorityRequired:true,status:'AWAITING_AUTHORITY'});const s=new CounterpartyOperationsService(d,{reasoning:{},outcomes:{}});const x=s.statusForPackage('EXP');assert.equal(x.phase,5);assert.equal(x.status,'AWAITING_AUTHORITY');assert.equal(x.awaitingAuthorityCount,1);});
