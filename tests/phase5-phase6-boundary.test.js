import assert from 'node:assert/strict';import test from 'node:test';import { CounterpartyOperationsService } from '../services/counterparty-operations-service.js';class D{constructor(){this.m=new Map([['COUNTERPARTY_OPERATION_CASE:C',{id:'C',caseId:'C',exportPackageId:'EXP',authorityRequired:true,status:'AWAITING_AUTHORITY'}]]);}list(t){return [...this.m.entries()].filter(([k])=>k.startsWith(`${t}:`)).map(([,v])=>structuredClone(v));}}
test('Phase 5 status preserves authority wait for downstream handoff',()=>{const s=new CounterpartyOperationsService(new D(),{reasoning:{},outcomes:{}});assert.equal(s.statusForPackage('EXP').status,'AWAITING_AUTHORITY');});
