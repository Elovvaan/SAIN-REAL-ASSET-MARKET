import assert from 'node:assert/strict';import test from 'node:test';import { CounterpartyOperationsStatusService } from '../services/counterparty-operations-status-service.js';
class D{constructor(){this.m=new Map();}k(t,i){return `${t}:${i}`;}list(t){return [...this.m.entries()].filter(([k])=>k.startsWith(`${t}:`)).map(([,v])=>structuredClone(v));}async put(t,i,p){this.m.set(this.k(t,i),structuredClone(p));}}
test('projects active counterparty cases for admin consumers',async()=>{const d=new D();await d.put('COUNTERPARTY_OPERATION_CASE','C1',{id:'C1',caseId:'C1',exportPackageId:'EXP-1',authorityRequired:true,status:'AWAITING_AUTHORITY'});const s=new CounterpartyOperationsStatusService(d);const list=await s.listActive();assert.equal(list.length,1);assert.equal(list[0].exportPackageId,'EXP-1');assert.equal(list[0].status,'AWAITING_AUTHORITY');});
