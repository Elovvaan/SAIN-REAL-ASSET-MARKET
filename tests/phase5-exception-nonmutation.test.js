import assert from 'node:assert/strict';import test from 'node:test';import { CounterpartyOperationsService } from '../services/counterparty-operations-service.js';class D{constructor(){this.m=new Map();}k(t,i){return `${t}:${i}`;}list(t){return [...this.m.entries()].filter(([k])=>k.startsWith(`${t}:`)).map(([,v])=>structuredClone(v));}async put(t,i,p){this.m.set(this.k(t,i),structuredClone(p));}}
test('processing exception records a case without changing package state',async()=>{const d=new D();await d.put('EXPORT_PACKAGE','EXP',{id:'EXP',exportPackageId:'EXP',state:'READY_FOR_SETTLEMENT_INSTRUCTION',amount:50000});await d.put('TRANSACTION_PARTICIPATION_EVENT','E',{id:'E',eventId:'E',windowId:'W',exportPackageId:'EXP',eventType:'PROCESSING_EXCEPTION_REPORTED',summary:'Processor rejected the reference.',details:{topic:'PACKAGE_REFERENCE'},createdAt:new Date().toISOString()});const s=new CounterpartyOperationsService(d,{reasoning:{reasonForExportPackage(){return{unresolvedFields:[],unresolvedServicingFields:[]};}},outcomes:{summary(){return{status:'EXCEPTION_REPORTED'};}}});await s.resolveParticipationEvent('E');const p=d.list('EXPORT_PACKAGE')[0];assert.equal(p.state,'READY_FOR_SETTLEMENT_INSTRUCTION');assert.equal(p.amount,50000);});
