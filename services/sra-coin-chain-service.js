const TYPE='SRA_COIN_CHAIN_PROJECTION';function active(p){return String(p?.state||'').toUpperCase()!=='RETIRED'&&String(p?.symbol||'').toUpperCase()==='SRA';}export class SraCoinChainService{constructor(domain,solana){this.domain=domain;this.solana=solana;this.hydrated=false;}async ensure(){if(!this.hydrated){await this.domain.hydrate([TYPE]);this.hydrated=true;}}supply(){return Number(this.domain.list('COIN_POSITION').filter(active).reduce((sum,p)=>sum+Number(p.quantity||0),0).toFixed(8));}async status(){await this.ensure();const platformSupply=this.supply(),onChain=this.domain.get(TYPE,'SRA-SOLANA')||null;return{symbol:'SRA',network:'SOLANA',platformSupply,onChain,synchronization:{platformSupply,issuedOnChainSupply:Number(onChain?.issuedOnChainSupply||0),pendingQuantity:Number(Math.max(0,platformSupply-Number(onChain?.issuedOnChainSupply||0)).toFixed(8)),state:!onChain?'NOT_ON_CHAIN':platformSupply>Number(onChain.issuedOnChainSupply||0)?'SYNC_AVAILABLE':platformSupply===Number(onChain.issuedOnChainSupply||0)?'SYNCHRONIZED':'RECONCILIATION_REQUIRED'}};}async putOnChain(_input={},actorId=null){await this.ensure();const supply=this.supply();if(supply<=0)throw new Error('No active SRA coin supply is available to put on chain.');const existing=this.domain.get(TYPE,'SRA-SOLANA');if(existing&&Number(existing.issuedOnChainSupply||0)>supply)throw new Error('On-chain issued SRA exceeds current platform supply; reconciliation is required before another mint.');const result=await this.solana.createSraMint({issuanceId:`SRA-SOLANA-${supply.toFixed(8)}`,authorizedSupply:supply,decimals:8});const now=new Date().toISOString();const projection={...(existing||{}),projectionId:'SRA-SOLANA',symbol:'SRA',network:'SOLANA',mintAddress:result.mintAddress,platformTokenAccount:result.platformTokenAccount,platformSupply:supply,authorizedOnChainSupply:supply,issuedOnChainSupply:result.issuedSupply,transactionSignature:result.transactionSignature||existing?.transactionSignature||null,lastMintedQuantity:Number(result.mintedQuantity||0),status:'ACTIVE',authoritativeRecordType:'COIN_POSITION_AGGREGATE',createdBy:existing?.createdBy||actorId,createdAt:existing?.createdAt||now,updatedBy:actorId,updatedAt:now,lastSynchronizedAt:now};await this.domain.put(TYPE,projection.projectionId,projection,{actorId,eventType:existing?'SRA_COIN_ON_CHAIN_SUPPLY_SYNCHRONIZED':'SRA_COIN_PUT_ON_CHAIN'});await this.domain.lifecycle({objectType:TYPE,objectId:projection.projectionId,eventType:existing?'SRA_COIN_ON_CHAIN_SUPPLY_SYNCHRONIZED':'SRA_COIN_PUT_ON_CHAIN',actorId,payload:{platformSupply:supply,issuedOnChainSupply:result.issuedSupply,mintedQuantity:Number(result.mintedQuantity||0),mintAddress:result.mintAddress,transactionSignature:result.transactionSignature||null}});return projection;}async send(input={},actorId=null){await this.ensure();const projection=this.domain.get(TYPE,'SRA-SOLANA');if(!projection)throw new Error('SRA has not been put on chain yet.');const result=await this.solana.sendSra(input);await this.domain.lifecycle({objectType:TYPE,objectId:projection.projectionId,eventType:'SRA_COIN_ON_CHAIN_TRANSFER',actorId,payload:{destinationAddress:result.destinationAddress,amount:result.amount,transactionSignature:result.transactionSignature}});return result;}}