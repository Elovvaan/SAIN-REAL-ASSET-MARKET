import crypto from 'node:crypto';
import { AgentCompensationService } from './agent-compensation-service.js';

const TYPES=Object.freeze({AGENT:'SRA_AGENT_WORKER',WORK:'SRA_AGENT_WORK_ORDER',COMPENSATION:'SRA_AGENT_COMPENSATION_RECORD'});
const WORK_STATES=new Set(['ASSIGNED','IN_PROGRESS','COMPLETED','ACCEPTED','REJECTED','CANCELLED']);
const PAYMENT_STATES=new Set(['EARNED','AUTHORIZED','PAID','VOID']);
const PROTECTED_EXECUTION_CLASSES=new Set(['FINANCIAL_AUTHORIZATION','EXTERNAL_SETTLEMENT','ON_CHAIN_EXECUTION','INSTRUMENT_ISSUANCE']);
const now=()=>new Date().toISOString();
const id=(prefix)=>`${prefix}-${crypto.randomUUID().split('-')[0].toUpperCase()}`;
const required=(value,field)=>{const v=String(value||'').trim();if(!v)throw new Error(`${field} is required.`);return v;};
const money=(value,field='amount')=>{const n=Number(value);if(!Number.isFinite(n)||n<0)throw new Error(`${field} must be zero or greater.`);return Number(n.toFixed(2));};
const stableWorkId=(agentId,entry)=>`WORK-${crypto.createHash('sha256').update(`${agentId}|${entry.stage||''}|${entry.id||''}|${entry.nextAction||''}`).digest('hex').slice(0,12).toUpperCase()}`;
const QUEUE_AGENT=Object.freeze({ORDER_INTENT:'SRA-ORDER-AGENT',MATCH_REVIEW:'SRA-ORDER-AGENT',RESERVATION:'SRA-ORDER-AGENT',ALLOCATION:'SRA-SETTLEMENT-AGENT',SETTLEMENT:'SRA-EXPORT-AGENT',EXPORT_PACKAGE:'SRA-EXPORT-AGENT',TRANSFER_INSTRUCTION:'SRA-EXPORT-AGENT',EXTERNAL_EXECUTION:'SRA-EXPORT-AGENT',TRANSFER_EXCEPTION:'SRA-EXPORT-AGENT',EXPORT_EXCEPTION:'SRA-EXPORT-AGENT'});

export class AgentWorkforceService {
  constructor({domain,database=null}){this.domain=domain;this.database=database;this.compensation=new AgentCompensationService();}
  async initialize(){await this.domain.hydrate(Object.values(TYPES));return this.status();}
  status(){return{service:'SRA_AGENT_WORKFORCE',agents:this.domain.list(TYPES.AGENT).length,workOrders:this.domain.list(TYPES.WORK).length,compensationRecords:this.domain.list(TYPES.COMPENSATION).length,authority:'GOVERNED_DELEGATION',economicModel:'WORK_PERFORMED_AND_ACCEPTED_BEFORE_COMPENSATION',compensationPolicy:this.compensation.policy()};}
  listAgents(filters={}){return this.domain.list(TYPES.AGENT).filter(a=>(!filters.state||a.state===filters.state)&&(!filters.role||a.role===filters.role));}
  getAgent(agentId){return this.domain.get(TYPES.AGENT,agentId);}
  compensationSchedule(agentId){const q=this.compensation.quote(agentId);return{amount:q.amount,currency:q.currency,basis:q.basis,humanEquivalentRole:q.humanEquivalentRole||null,benchmarkHourlyUsd:q.benchmarkHourlyUsd||null,benchmarkAnnualUsd:q.benchmarkAnnualUsd||null,humanEquivalentMinutes:q.humanEquivalentMinutes||null,effectiveDate:q.effectiveDate||null};}
  async registerAgent(input={},actor={}){const agentId=input.agentId||id('AGENT');const schedule=this.compensationSchedule(agentId);const record={agentId,name:required(input.name,'name'),role:required(input.role,'role').toUpperCase(),description:String(input.description||'').trim()||null,capabilities:[...new Set((input.capabilities||[]).map(v=>String(v).trim().toUpperCase()).filter(Boolean))],executionClasses:[...new Set((input.executionClasses||['READ_ONLY','SAFE_PREPARATION']).map(v=>String(v).trim().toUpperCase()).filter(Boolean))],walletReference:input.walletReference||null,paymentAccountReference:input.paymentAccountReference||null,compensationSchedule:schedule,state:'ACTIVE',createdBy:actor.id||null,createdAt:now(),updatedAt:now()};await this.domain.put(TYPES.AGENT,agentId,record,{actorId:actor.id||null,eventType:'SRA_AGENT_WORKER_REGISTERED'});return record;}
  async synchronizeOperatingAgents(agentDefinitions=[],actor={}){const created=[],existing=[];for(const definition of agentDefinitions){if(!definition?.agentId)continue;const current=this.getAgent(definition.agentId);if(current){const schedule=this.compensationSchedule(definition.agentId);const updated={...current,compensationSchedule:schedule,updatedAt:now()};await this.domain.put(TYPES.AGENT,definition.agentId,updated,{actorId:actor.id||null,eventType:'SRA_AGENT_COMPENSATION_SCHEDULE_SYNCHRONIZED'});existing.push(updated);continue;}created.push(await this.registerAgent({agentId:definition.agentId,name:definition.agentId,role:definition.agentType||'SRA_AGENT',description:`Existing SRA operating agent for ${definition.scope||'platform operations'}.`,capabilities:definition.capabilities||[],executionClasses:['READ_ONLY','SAFE_PREPARATION']},actor));}return{created,existing,agentCount:created.length+existing.length};}
  listWork(filters={}){return this.domain.list(TYPES.WORK).filter(w=>(!filters.agentId||w.agentId===filters.agentId)&&(!filters.state||w.state===filters.state)&&(!filters.opportunityId||w.opportunityId===filters.opportunityId));}
  getWork(workOrderId){return this.domain.get(TYPES.WORK,workOrderId);}
  async assignWork(input={},actor={}){const agent=this.getAgent(required(input.agentId,'agentId'));if(!agent||agent.state!=='ACTIVE')throw new Error('Active SRA agent worker not found.');const executionClass=required(input.executionClass||'SAFE_PREPARATION','executionClass').toUpperCase();if(!agent.executionClasses.includes(executionClass))throw new Error('Agent is not delegated for this execution class.');const workOrderId=input.workOrderId||id('WORK');const record={workOrderId,agentId:agent.agentId,role:agent.role,title:required(input.title,'title'),objective:required(input.objective,'objective'),executionClass,requiresAdministratorAuthorization:PROTECTED_EXECUTION_CLASSES.has(executionClass),opportunityId:input.opportunityId||null,instrumentId:input.instrumentId||null,settlementInstructionId:input.settlementInstructionId||null,onChainInstructionId:input.onChainInstructionId||null,sourceRecordId:input.sourceRecordId||null,sourceStage:input.sourceStage||null,requestedAction:input.requestedAction||null,positionId:input.positionId||null,listingId:input.listingId||null,participantId:input.participantId||null,serviceFeePayerId:input.serviceFeePayerId||null,serviceFeePayerType:input.serviceFeePayerType?String(input.serviceFeePayerType).toUpperCase():null,authorizedCompensation:money(input.authorizedCompensation||0,'authorizedCompensation'),currency:String(input.currency||'USD').toUpperCase(),compensationBasis:input.compensationBasis||null,humanEquivalentRole:input.humanEquivalentRole||null,humanEquivalentMinutes:Number(input.humanEquivalentMinutes||0)||null,compensationEffectiveDate:input.compensationEffectiveDate||null,state:'ASSIGNED',assignedBy:actor.id||null,assignedAt:now(),history:[{state:'ASSIGNED',at:now(),actorId:actor.id||null}]};await this.domain.put(TYPES.WORK,workOrderId,record,{actorId:actor.id||null,eventType:'SRA_AGENT_WORK_ASSIGNED'});return record;}
  async startWork(workOrderId,actor={}){const w=this.getWork(workOrderId);if(!w)throw new Error('Agent work order not found.');if(w.state!=='ASSIGNED')throw new Error(`Work cannot start from ${w.state}.`);const u={...w,state:'IN_PROGRESS',startedAt:now(),updatedAt:now(),history:[...(w.history||[]),{state:'IN_PROGRESS',at:now(),actorId:actor.id||w.agentId}]};await this.domain.put(TYPES.WORK,workOrderId,u,{actorId:actor.id||w.agentId,eventType:'SRA_AGENT_WORK_STARTED'});return u;}
  async completeWork(workOrderId,input={},actor={}){const w=this.getWork(workOrderId);if(!w)throw new Error('Agent work order not found.');if(!['ASSIGNED','IN_PROGRESS'].includes(w.state))throw new Error(`Work cannot complete from ${w.state}.`);const resultReference=required(input.resultReference,'resultReference');const u={...w,state:'COMPLETED',resultReference,resultSummary:String(input.resultSummary||'').trim()||null,transactionReference:input.transactionReference||null,completedAt:now(),updatedAt:now(),history:[...(w.history||[]),{state:'COMPLETED',at:now(),actorId:actor.id||w.agentId}]};await this.domain.put(TYPES.WORK,workOrderId,u,{actorId:actor.id||w.agentId,eventType:'SRA_AGENT_WORK_COMPLETED'});return u;}
  async runOperationalQueue(queueSnapshot={},actor={id:'SRA_AGENT_OS'}){const entries=[...(queueSnapshot.queue||[]),...(queueSnapshot.exceptions||[])],created=[],completed=[],skipped=[];for(const entry of entries){const agentId=QUEUE_AGENT[entry.stage];if(!agentId||!this.getAgent(agentId)){skipped.push({entryId:entry.id,stage:entry.stage,reason:'NO_REGISTERED_WORKFORCE_AGENT'});continue;}const workOrderId=stableWorkId(agentId,entry),prior=this.getWork(workOrderId);if(prior){skipped.push({workOrderId,state:prior.state,reason:'WORK_ALREADY_RECORDED'});continue;}const quote=this.compensation.quote(agentId);const work=await this.assignWork({workOrderId,agentId,title:`Review ${entry.stage}: ${entry.nextAction||'NEXT_ACTION'}`,objective:entry.coinAgent?.explanation||entry.explanation||`Review SRA operational queue item ${entry.id}.`,executionClass:'SAFE_PREPARATION',sourceRecordId:entry.id,sourceStage:entry.stage,requestedAction:entry.nextAction||null,instrumentId:entry.instrumentId||null,positionId:entry.positionId||null,listingId:entry.listingId||null,participantId:entry.participantId||null,serviceFeePayerId:entry.participantId||null,serviceFeePayerType:entry.participantId?'PARTICIPANT':null,authorizedCompensation:quote.amount,currency:quote.currency,compensationBasis:quote.basis,humanEquivalentRole:quote.humanEquivalentRole,humanEquivalentMinutes:quote.humanEquivalentMinutes,compensationEffectiveDate:quote.effectiveDate},actor);created.push(work);await this.startWork(workOrderId,{id:agentId});completed.push(await this.completeWork(workOrderId,{resultReference:`SRA_OPERATIONS_QUEUE:${entry.stage}:${entry.id}:${entry.state||'CURRENT'}`,resultSummary:entry.coinAgent?.explanation||entry.explanation||`Reviewed ${entry.stage} and identified ${entry.nextAction||'the next governed action'}.`},{id:agentId}));}return{generatedAt:now(),queueState:queueSnapshot.state||null,createdCount:created.length,completedCount:completed.length,skippedCount:skipped.length,created,completed,skipped};}
  async acceptWork(workOrderId,input={},actor={}){const w=this.getWork(workOrderId);if(!w)throw new Error('Agent work order not found.');if(w.state!=='COMPLETED')throw new Error('Only completed agent work can be accepted.');if(String(input.approval||'').toUpperCase()!=='ACCEPT')throw new Error('Explicit administrator acceptance is required.');const acceptedAt=now(),u={...w,state:'ACCEPTED',acceptedBy:actor.id||null,acceptedAt,updatedAt:acceptedAt,history:[...(w.history||[]),{state:'ACCEPTED',at:acceptedAt,actorId:actor.id||null}]};const compensationId=id('COMP'),comp={compensationId,workOrderId:w.workOrderId,agentId:w.agentId,amount:w.authorizedCompensation,currency:w.currency,basis:w.compensationBasis||'ACCEPTED_AGENT_WORK',humanEquivalentRole:w.humanEquivalentRole||null,humanEquivalentMinutes:w.humanEquivalentMinutes||null,compensationEffectiveDate:w.compensationEffectiveDate||null,state:'EARNED',earnedAt:acceptedAt,authorizedBy:null,paidAt:null,paymentReference:null};if(typeof this.domain.atomicPut==='function')await this.domain.atomicPut([{type:TYPES.WORK,id:w.workOrderId,payload:u,actorId:actor.id||null,eventType:'SRA_AGENT_WORK_ACCEPTED'},{type:TYPES.COMPENSATION,id:compensationId,payload:comp,actorId:actor.id||null,eventType:'SRA_AGENT_COMPENSATION_EARNED'}]);else{await this.domain.put(TYPES.WORK,w.workOrderId,u,{actorId:actor.id||null,eventType:'SRA_AGENT_WORK_ACCEPTED'});await this.domain.put(TYPES.COMPENSATION,compensationId,comp,{actorId:actor.id||null,eventType:'SRA_AGENT_COMPENSATION_EARNED'});}return{work:u,compensation:comp};}
  listCompensation(filters={}){return this.domain.list(TYPES.COMPENSATION).filter(c=>(!filters.agentId||c.agentId===filters.agentId)&&(!filters.state||c.state===filters.state));}
  async transitionCompensation(compensationId,targetState,input={},actor={}){const c=this.domain.get(TYPES.COMPENSATION,compensationId);if(!c)throw new Error('Agent compensation record not found.');const state=required(targetState,'state').toUpperCase();if(!PAYMENT_STATES.has(state))throw new Error(`Unsupported compensation state: ${state}.`);const allowed={EARNED:['AUTHORIZED','VOID'],AUTHORIZED:['PAID','VOID'],PAID:[],VOID:[]};if(!allowed[c.state]?.includes(state))throw new Error(`Invalid compensation transition: ${c.state} -> ${state}.`);if(state==='AUTHORIZED'&&String(input.approval||'').toUpperCase()!=='AUTHORIZE')throw new Error('Explicit administrator authorization is required.');if(state==='PAID'&&!input.paymentReference)throw new Error('paymentReference is required when compensation is paid.');const u={...c,state,authorizedBy:state==='AUTHORIZED'?(actor.id||null):c.authorizedBy,authorizedAt:state==='AUTHORIZED'?now():c.authorizedAt||null,paidAt:state==='PAID'?now():c.paidAt||null,paymentReference:state==='PAID'?input.paymentReference:c.paymentReference||null,updatedAt:now()};await this.domain.put(TYPES.COMPENSATION,compensationId,u,{actorId:actor.id||null,eventType:`SRA_AGENT_COMPENSATION_${state}`});return u;}
}

export const SRA_AGENT_WORKFORCE_TYPES=TYPES;
