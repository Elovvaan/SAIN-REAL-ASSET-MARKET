import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';test('SRA chain projection identifies SRA as authoritative',()=>{const service=fs.readFileSync(new URL('../services/sra-coin-chain-service.js',import.meta.url),'utf8');assert.match(service,/authoritativeRecordType:'COIN_POSITION_AGGREGATE'/);assert.match(service,/authorizedOnChainSupply:supply/);});
