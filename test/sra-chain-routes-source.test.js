import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';test('SRA chain routes use the SRA coin chain service',()=>{const r=fs.readFileSync(new URL('../routes/on-chain-projection-router.js',import.meta.url),'utf8');assert.match(r,/new SraCoinChainService\(service\.domain,solana\)/);assert.match(r,/\/solana\/sra\/mint/);assert.match(r,/\/solana\/sra\/transfers/);});
