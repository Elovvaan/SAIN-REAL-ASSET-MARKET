import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');test('SRA on-chain path stays tied to coin positions',()=>{const s=read('services/sra-coin-chain-service.js');assert.match(s,/domain\.list\('COIN_POSITION'\)/);assert.match(s,/authoritativeRecordType:'COIN_POSITION_AGGREGATE'/);});test('admin path is put on chain then send to address',()=>{const ui=read('public/admin/admin-solana-transfer.js');for(const phrase of ['Put SRA On Chain','Send SRA','Destination Solana address'])assert.ok(ui.includes(phrase));});test('executor uses normal Solana and SPL token transfers',()=>{const w=read('external/orca-executor/sra-token-worker.js');assert.match(w,/SystemProgram\.transfer/);assert.match(w,/createMint/);assert.match(w,/mintTo/);assert.match(w,/transfer\(/);});test('simple Solana controls load before optional DEX controls',()=>{const b=read('public/admin/admin-bootstrap.js');assert.ok(b.indexOf('/admin/admin-solana-transfer.js')<b.indexOf('/admin/admin-external-dex-adapter.js'));});
