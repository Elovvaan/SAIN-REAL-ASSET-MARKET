import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';test('SRA mint and transfers are idempotent',()=>{const w=fs.readFileSync(new URL('../external/orca-executor/sra-token-worker.js',import.meta.url),'utf8');assert.match(w,/Idempotency-Key must equal issuanceId/);assert.match(w,/Idempotency-Key must equal transferId/);assert.match(w,/sra_chain_transfers/);});
