import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';test('SRA uses one persisted Solana mint',()=>{const w=fs.readFileSync(new URL('../external/orca-executor/sra-token-worker.js',import.meta.url),'utf8');assert.match(w,/symbol TEXT PRIMARY KEY/);assert.match(w,/WHERE symbol='SRA'/);assert.match(w,/SRA mint already exists with a different authorized supply/);});
