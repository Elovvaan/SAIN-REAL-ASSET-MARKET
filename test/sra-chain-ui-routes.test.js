import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';test('Solana admin calls only direct Solana routes for basic sends',()=>{const u=fs.readFileSync(new URL('../public/admin/admin-solana-transfer.js',import.meta.url),'utf8');assert.match(u,/\/api\/on-chain\/solana\/wallet/);assert.match(u,/\/api\/on-chain\/solana\/sra/);assert.doesNotMatch(u,/\/dex\//);});
