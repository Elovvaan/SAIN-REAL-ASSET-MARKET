import test from 'node:test';import assert from 'node:assert/strict';import { SraCoinChainService } from '../services/sra-coin-chain-service.js';test('SRA chain supply is the active SRA coin-position aggregate',()=>{const records=[{symbol:'SRA',state:'REPRESENTED',quantity:97.17},{symbol:'SRA',state:'RETIRED',quantity:5},{symbol:'OTHER',state:'REPRESENTED',quantity:100}];const domain={list:t=>t==='COIN_POSITION'?records:[]};const service=new SraCoinChainService(domain,{});assert.equal(service.supply(),97.17);});
