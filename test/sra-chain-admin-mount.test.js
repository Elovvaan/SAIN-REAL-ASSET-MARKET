import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';test('Solana administration mounts before DEX',()=>{const b=fs.readFileSync(new URL('../public/admin/admin-bootstrap.js',import.meta.url),'utf8');const sol=b.indexOf('/admin/admin-solana-transfer.js'),dex=b.indexOf('/admin/admin-external-dex-adapter.js');assert.ok(sol>=0&&dex>sol);assert.match(b,/mountAdminSolanaTransfer/);});
