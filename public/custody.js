const custodyEscape=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const custodyMoney=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0});
function custodyFlow(items){return `<div class="custody-flow">${items.map((item,i)=>`${i?'<b>→</b>':''}<span>${custodyEscape(item.replaceAll('_',' '))}</span>`).join('')}</div>`}
async function renderCustody(){
  const root=document.querySelector('#view-root');document.querySelector('#page-title').textContent='Institutional Custody & Records';
  root.innerHTML='<div class="loading-state">Loading V9 institutional records…</div>';
  try{
    const response=await fetch('/api/custody');if(!response.ok)throw new Error();const d=await response.json();
    root.innerHTML=`<section class="custody-callout"><div><p class="eyebrow">V9 · INTERNAL INSTITUTIONAL LAYER</p><h2>Custody before capital</h2><p>Private evidence remains controlled while filing, collateral, settlement, setoff, discharge, and release are recorded behind the customer-facing V4V flow.</p></div><span class="badge watch">INTERNAL ONLY</span></section>
    <section class="metric-grid"><article class="metric-card"><span>Custody records</span><strong>${d.metrics.custodyRecords}</strong><small>Controlled institutional files</small></article><article class="metric-card"><span>Restricted documents</span><strong>${d.metrics.restrictedDocuments}</strong><small>Not public-facing</small></article><article class="metric-card"><span>Collateral schedules</span><strong>${d.metrics.collateralSchedules}</strong><small>Internal designation records</small></article><article class="metric-card"><span>Discharged positions</span><strong>${d.metrics.dischargedPositions}</strong><small>Accounting positions closed</small></article></section>
    <section class="panel"><div class="panel-header"><div><h2>Institutional workflow</h2><p>V4V intake through custody resolution.</p></div></div>${custodyFlow(d.workflow)}</section>
    <section class="content-grid"><div class="panel"><div class="panel-header"><div><h2>Custody Records</h2><p>Document control, access state, filing, and chain of custody.</p></div></div>${d.custodyRecords.map(r=>`<article class="custody-record"><div class="record-head"><div><strong>${custodyEscape(r.filingNumber)}</strong><p>${custodyEscape(r.id)} · Asset ${custodyEscape(r.assetId)}</p></div><span class="badge open">${custodyEscape(r.custodyStatus)}</span></div><div class="record-grid"><div><span>Access</span><strong>${custodyEscape(r.accessClass)}</strong></div><div><span>Documents</span><strong>${r.documentIds.length}</strong></div><div><span>Collateral state</span><strong>${custodyEscape(r.collateralState)}</strong></div><div><span>Schedule</span><strong>${custodyEscape(r.collateralScheduleId||'None')}</strong></div></div><div class="custody-events">${r.chainOfCustody.map(e=>`<div><span>${e.position}</span><strong>${custodyEscape(e.action)}</strong><small>${custodyEscape(e.actorId)} · ${new Date(e.at).toLocaleString()}</small></div>`).join('')}</div></article>`).join('')}</div>
    <div class="panel"><div class="panel-header"><div><h2>Collateral Schedules</h2><p>Internal controls inspired by custody-based collateral operations.</p></div></div>${d.collateralSchedules.map(s=>`<article class="schedule-card"><div class="record-head"><div><strong>${custodyEscape(s.filingNumber)}</strong><p>${custodyEscape(s.id)}</p></div><span class="badge watch">${custodyEscape(s.status)}</span></div><div class="record-grid"><div><span>Gross Verified Value</span><strong>${custodyMoney.format(s.grossVerifiedValue)}</strong></div><div><span>Designated amount</span><strong>${custodyMoney.format(s.designatedAmount)}</strong></div><div><span>Lendable value</span><strong>${s.lendableValue==null?'NOT SET':custodyMoney.format(s.lendableValue)}</strong></div><div><span>Reporting</span><strong>${custodyEscape(s.controls.reportingState)}</strong></div></div><p class="internal-note">${custodyEscape(s.note)}</p></article>`).join('')}</div></section>
    <section class="panel"><div class="panel-header"><div><h2>Discharge Filings</h2><p>Settlement, setoff, release, and the resulting closed accounting position.</p></div></div><div class="discharge-list">${d.dischargeRecords.map(r=>`<article class="discharge-record"><div class="record-head"><div><strong>${custodyEscape(r.filingNumber)}</strong><p>${custodyEscape(r.positionType)} · ${custodyEscape(r.projectId||r.assetId)}</p></div><span class="badge open">${custodyEscape(r.state)}</span></div><div class="discharge-equation"><div><span>Opening</span><strong>${custodyMoney.format(r.openingAmount)}</strong></div><b>−</b><div><span>Settlement</span><strong>${custodyMoney.format(r.valueApplied)}</strong></div><b>−</b><div><span>Setoff</span><strong>${custodyMoney.format(r.setoffApplied)}</strong></div><b>−</b><div><span>Release</span><strong>${custodyMoney.format(r.releasedAmount)}</strong></div><b>=</b><div><span>Remaining</span><strong>${custodyMoney.format(r.remainingAmount)}</strong></div></div><small>Method ${custodyEscape(r.method)} · Instruments ${r.instrumentIds.map(custodyEscape).join(', ')} · Posted ${r.postedAt?new Date(r.postedAt).toLocaleString():'Pending'}</small></article>`).join('')}</div></section>
    <section class="content-grid"><div class="panel"><div class="panel-header"><div><h2>Customer-facing</h2><p>Only approved status and representation.</p></div></div><div class="policy-list">${d.policy.customerFacing.map(x=>`<span>✓ ${custodyEscape(x)}</span>`).join('')}</div></div><div class="panel"><div class="panel-header"><div><h2>Institutional-only</h2><p>Protected company mechanisms.</p></div></div><div class="policy-list">${d.policy.internalOnly.map(x=>`<span>• ${custodyEscape(x)}</span>`).join('')}</div></div></section>`;
  }catch{root.innerHTML='<div class="empty-view"><h2>V9 custody service unavailable</h2><p>Refresh after Railway completes the deployment.</p></div>'}
}
const custodyOld=document.querySelector('[data-view="custody"]');
if(custodyOld){const button=custodyOld.cloneNode(true);custodyOld.replaceWith(button);button.addEventListener('click',()=>{document.querySelectorAll('.nav-item').forEach(i=>i.classList.remove('active'));button.classList.add('active');renderCustody()})}
