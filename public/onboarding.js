const onboardingState={step:0,configuration:null,result:null,data:{identity:{name:'',region:'',description:'',externalReference:''},documents:[{type:'TITLE_OR_DEED',name:'',reference:''}],ownership:{ownerName:'',ownershipType:'BUSINESS',contactEmail:'',contactPhone:''},classification:'REAL_ESTATE',verification:{method:'DOCUMENT_REVIEW',verifierName:'',attested:false}}};
const onboardingSteps=[
  {key:'REGISTER',title:'Register Asset',help:'Start the permanent asset relationship.'},
  {key:'IDENTITY',title:'Identity',help:'Describe what the asset is and where it operates.'},
  {key:'DOCUMENTS',title:'Documents',help:'Record the supporting documents that identify the asset.'},
  {key:'OWNERSHIP',title:'Ownership',help:'Connect the asset to its owner participant.'},
  {key:'CLASSIFICATION',title:'Classification',help:'Place the asset in the correct productive class.'},
  {key:'VERIFICATION',title:'Verification',help:'Confirm the onboarding record before creation.'},
  {key:'CREATED',title:'Asset Account Created',help:'The permanent Asset Account is now active.'}
];
const onboardEscape=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
function onboardingProgress(){return `<div class="wizard-progress">${onboardingSteps.map((s,i)=>`<div class="wizard-step ${i===onboardingState.step?'active':i<onboardingState.step?'complete':''}">${i<onboardingState.step?'✓ ':''}${onboardEscape(s.title)}</div>`).join('')}</div>`}
function field(label,name,value,type='text',full=false,placeholder=''){return `<div class="form-field ${full?'full':''}"><label for="ob-${name}">${onboardEscape(label)}</label><input id="ob-${name}" data-onboard-field="${name}" type="${type}" value="${onboardEscape(value)}" placeholder="${onboardEscape(placeholder)}"></div>`}
function registerStep(){return `<div class="wizard-heading"><span>STEP 1 OF 7</span><h2>Register the asset</h2><p>This begins the permanent SRA Asset Account workflow. Nothing is published to the marketplace during onboarding.</p></div><div class="review-grid"><div class="review-block"><span>Workflow</span><strong>Identity → Documents → Ownership → Classification → Verification</strong></div><div class="review-block"><span>Result</span><strong>Permanent Asset Account + Lifecycle Record</strong></div><div class="review-block"><span>Initial state</span><strong>PENDING_VERIFIED_VALUE</strong></div><div class="review-block"><span>Next workflow</span><strong>Verified Value baseline</strong></div></div>`}
function identityStep(){const d=onboardingState.data.identity;return `<div class="wizard-heading"><span>STEP 2 OF 7</span><h2>Asset identity</h2><p>Give the asset a clear identity that remains attached to its lifecycle.</p></div><div class="form-grid">${field('Asset name','identity.name',d.name,'text',false,'North District Market')}${field('Region or operating area','identity.region',d.region,'text',false,'Ogden, Utah')}${field('External reference','identity.externalReference',d.externalReference,'text',true,'Parcel, registration, internal ID, or other reference')}<div class="form-field full"><label for="ob-description">Description</label><textarea id="ob-description" data-onboard-field="identity.description" rows="5" placeholder="Describe the productive asset and its current purpose.">${onboardEscape(d.description)}</textarea></div></div>`}
function documentsStep(){const types=onboardingState.configuration?.documentTypes||['OTHER'];return `<div class="wizard-heading"><span>STEP 3 OF 7</span><h2>Supporting documents</h2><p>Record document references now. File storage will connect to these records in a later build.</p></div><div class="document-editor" id="document-editor">${onboardingState.data.documents.map((doc,i)=>`<div class="document-row"><div class="form-field"><label>Document type</label><select data-doc-field="type" data-doc-index="${i}">${types.map(t=>`<option value="${t}" ${doc.type===t?'selected':''}>${t.replaceAll('_',' ')}</option>`).join('')}</select></div><div class="form-field"><label>Document name</label><input data-doc-field="name" data-doc-index="${i}" value="${onboardEscape(doc.name)}" placeholder="Recorded deed"></div><div class="form-field"><label>Reference or location</label><input data-doc-field="reference" data-doc-index="${i}" value="${onboardEscape(doc.reference)}" placeholder="Document number or storage reference"></div><button type="button" class="danger-button" data-remove-doc="${i}">Remove</button></div>`).join('')}</div><button type="button" class="secondary-button" id="add-onboarding-document">Add another document</button>`}
function ownershipStep(){const d=onboardingState.data.ownership;const types=onboardingState.configuration?.ownershipTypes||['OTHER'];return `<div class="wizard-heading"><span>STEP 4 OF 7</span><h2>Ownership</h2><p>The owner becomes an SRA Participant wearing the Asset Owner hat.</p></div><div class="form-grid">${field('Owner or organization name','ownership.ownerName',d.ownerName,'text',false,'North District Holdings')}<div class="form-field"><label for="ob-ownership-type">Ownership type</label><select id="ob-ownership-type" data-onboard-field="ownership.ownershipType">${types.map(t=>`<option value="${t}" ${d.ownershipType===t?'selected':''}>${t.replaceAll('_',' ')}</option>`).join('')}</select></div>${field('Contact email','ownership.contactEmail',d.contactEmail,'email',false,'owner@example.com')}${field('Contact phone','ownership.contactPhone',d.contactPhone,'text',false,'Optional')}</div>`}
function classificationStep(){const classes=onboardingState.configuration?.classifications||[];return `<div class="wizard-heading"><span>STEP 5 OF 7</span><h2>Asset classification</h2><p>Classification determines the future Verified Value rules and marketplace workflows available to the asset.</p></div><div class="form-field"><label for="ob-classification">Classification</label><select id="ob-classification" data-onboard-field="classification">${classes.map(c=>`<option value="${c}" ${onboardingState.data.classification===c?'selected':''}>${c.replaceAll('_',' ')}</option>`).join('')}</select></div><div class="review-block"><span>Selected class</span><strong>${onboardEscape(onboardingState.data.classification.replaceAll('_',' '))}</strong></div>`}
function verificationStep(){const d=onboardingState.data;return `<div class="wizard-heading"><span>STEP 6 OF 7</span><h2>Verification and creation</h2><p>Review the onboarding record and attest that the information is ready to create the permanent Asset Account.</p></div><div class="review-grid"><div class="review-block"><span>Asset</span><strong>${onboardEscape(d.identity.name||'Not entered')}</strong><small>${onboardEscape(d.identity.region)}</small></div><div class="review-block"><span>Owner</span><strong>${onboardEscape(d.ownership.ownerName||'Not entered')}</strong><small>${onboardEscape(d.ownership.ownershipType)}</small></div><div class="review-block"><span>Classification</span><strong>${onboardEscape(d.classification.replaceAll('_',' '))}</strong></div><div class="review-block"><span>Documents</span><strong>${d.documents.length} recorded</strong></div></div><div class="form-grid">${field('Verifier or reviewer name','verification.verifierName',d.verification.verifierName,'text',false,'Self-attested onboarding')}<div class="form-field"><label for="ob-verification-method">Method</label><select id="ob-verification-method" data-onboard-field="verification.method"><option value="DOCUMENT_REVIEW" ${d.verification.method==='DOCUMENT_REVIEW'?'selected':''}>Document review</option><option value="PARTICIPANT_ATTESTATION" ${d.verification.method==='PARTICIPANT_ATTESTATION'?'selected':''}>Participant attestation</option><option value="INSTITUTIONAL_CONFIRMATION" ${d.verification.method==='INSTITUTIONAL_CONFIRMATION'?'selected':''}>Institutional confirmation</option></select></div></div><label class="attestation"><input type="checkbox" id="onboarding-attestation" ${d.verification.attested?'checked':''}><span>I attest that the asset identity, documents, ownership, and classification are ready to be recorded in SRA.</span></label><div id="onboarding-error"></div>`}
function createdStep(){const r=onboardingState.result;return `<div class="created-state"><div class="created-symbol">✓</div><div><p class="eyebrow">PERMANENT ASSET ACCOUNT</p><h2>Asset Account created</h2><div class="created-id">${onboardEscape(r.assetAccount.id)}</div></div><div class="created-summary"><div class="review-block"><span>Asset</span><strong>${onboardEscape(r.assetAccount.name)}</strong></div><div class="review-block"><span>Status</span><strong>${onboardEscape(r.assetAccount.status)}</strong></div><div class="review-block"><span>Owner Participant</span><strong>${onboardEscape(r.ownerParticipant.id)}</strong></div><div class="review-block"><span>Lifecycle Record</span><strong>${onboardEscape(r.lifecycleRecord.id)}</strong></div><div class="review-block"><span>Application</span><strong>${onboardEscape(r.applicationId)}</strong></div><div class="review-block"><span>Next action</span><strong>${onboardEscape(r.nextAction)}</strong></div></div><button type="button" class="primary-button" id="onboard-another">Register another asset</button></div>`}
function stepBody(){return [registerStep,identityStep,documentsStep,ownershipStep,classificationStep,verificationStep,createdStep][onboardingState.step]()}
function renderOnboarding(){const root=document.querySelector('#view-root');document.querySelector('#page-title').textContent='Asset Onboarding';root.innerHTML=`<section class="onboarding-shell"><div class="onboarding-intro"><div><p class="eyebrow">PHASE TWO</p><h2>Register a productive asset</h2><p>The first customer workflow creates the permanent identity the rest of SRA builds upon.</p></div><span class="badge open">DOMAIN CORE ACTIVE</span></div>${onboardingProgress()}<section class="wizard-card">${stepBody()}${onboardingState.step<6?`<div class="wizard-actions"><button type="button" class="secondary-button" id="wizard-back" ${onboardingState.step===0?'disabled':''}>Back</button><button type="button" class="primary-button" id="wizard-next">${onboardingState.step===5?'Create Asset Account':'Continue'}</button></div>`:''}</section></section>`;bindOnboarding()}
function setNested(path,value){const parts=path.split('.');let target=onboardingState.data;for(let i=0;i<parts.length-1;i++)target=target[parts[i]];target[parts.at(-1)]=value}
function captureFields(){document.querySelectorAll('[data-onboard-field]').forEach(el=>{setNested(el.dataset.onboardField,el.value)});document.querySelectorAll('[data-doc-field]').forEach(el=>{onboardingState.data.documents[Number(el.dataset.docIndex)][el.dataset.docField]=el.value});const attest=document.querySelector('#onboarding-attestation');if(attest)onboardingState.data.verification.attested=attest.checked}
function validateStep(){const d=onboardingState.data;if(onboardingState.step===1&&(!d.identity.name.trim()||!d.identity.region.trim()))return 'Asset name and region are required.';if(onboardingState.step===2&&(!d.documents.length||d.documents.some(x=>!x.name.trim())))return 'Each document needs a name.';if(onboardingState.step===3&&!d.ownership.ownerName.trim())return 'Owner name is required.';if(onboardingState.step===5&&!d.verification.attested)return 'The verification attestation must be checked.';return ''}
async function createAssetAccount(){const box=document.querySelector('#onboarding-error');box.innerHTML='';try{const response=await fetch('/api/onboarding/assets',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(onboardingState.data)});const result=await response.json();if(!response.ok){box.innerHTML=`<div class="wizard-error">${onboardEscape((result.errors||['Unable to create Asset Account.']).join(' '))}</div>`;return}onboardingState.result=result;onboardingState.step=6;renderOnboarding()}catch{box.innerHTML='<div class="wizard-error">The onboarding service is unavailable. Refresh after Railway completes deployment.</div>'}}
function bindOnboarding(){document.querySelector('#wizard-back')?.addEventListener('click',()=>{captureFields();onboardingState.step=Math.max(0,onboardingState.step-1);renderOnboarding()});document.querySelector('#wizard-next')?.addEventListener('click',async()=>{captureFields();const error=validateStep();if(error){const box=document.querySelector('#onboarding-error');if(box)box.innerHTML=`<div class="wizard-error">${onboardEscape(error)}</div>`;else alert(error);return}if(onboardingState.step===5){await createAssetAccount();return}onboardingState.step++;renderOnboarding()});document.querySelector('#add-onboarding-document')?.addEventListener('click',()=>{captureFields();onboardingState.data.documents.push({type:'OTHER',name:'',reference:''});renderOnboarding()});document.querySelectorAll('[data-remove-doc]').forEach(btn=>btn.addEventListener('click',()=>{captureFields();onboardingState.data.documents.splice(Number(btn.dataset.removeDoc),1);if(!onboardingState.data.documents.length)onboardingState.data.documents.push({type:'OTHER',name:'',reference:''});renderOnboarding()}));document.querySelector('#onboard-another')?.addEventListener('click',()=>{onboardingState.step=0;onboardingState.result=null;onboardingState.data={identity:{name:'',region:'',description:'',externalReference:''},documents:[{type:'TITLE_OR_DEED',name:'',reference:''}],ownership:{ownerName:'',ownershipType:'BUSINESS',contactEmail:'',contactPhone:''},classification:'REAL_ESTATE',verification:{method:'DOCUMENT_REVIEW',verifierName:'',attested:false}};renderOnboarding()})}
async function initializeOnboarding(){try{const response=await fetch('/api/onboarding/configuration');if(response.ok)onboardingState.configuration=await response.json()}catch{}const old=document.querySelector('[data-view="onboarding"]');if(old){const button=old.cloneNode(true);old.replaceWith(button);button.addEventListener('click',()=>{document.querySelectorAll('.nav-item').forEach(item=>item.classList.remove('active'));button.classList.add('active');onboardingState.step=onboardingState.result?6:0;renderOnboarding()})}}
initializeOnboarding();
